package solid.srp;

public class NotificationService {

	/**
	 * In future, if we need to add a different medium, then this class needs to be
	 * modified which clearly violates the Open Closed Principle.
	 * 
	 * 
	 */

	// so is the below class
	public void setOTP(String medium) {

		if (medium.equals("email")) {
			//
		}
	}
}
